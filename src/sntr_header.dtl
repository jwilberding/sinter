%% This template is generated by
%% {{engine.name}}-{{engine.version}}. Do not edit by hand.

-record({{ object.name }},
        {'$version$'={{ object.version }} :: non_neg_integer(), {% for field in fields %}
          {% include "sntr_field.dtl" %}{% if not forloop.last %},{% endif %} {% endfor %}
        }).
